apiVersion: v1
kind: ConfigMap
metadata:
  name: lms-config
  namespace: lms
  labels:
    app: stitch-lms
data:
  NODE_ENV: "production"
  PORT: "5173"
  DB_HOST: "postgres-service"
  DB_PORT: "5432"
  DB_NAME: "lms_db"
  CORS_ORIGIN: "*"
  # Add other non-sensitive configuration here

